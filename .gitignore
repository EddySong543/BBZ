# === IDE and Editor ===
.vs/
.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini

# === Local Tooling (per-machine paths / proxy configs / launcher scripts) ===
# Never commit scripts containing local absolute paths, proxy IPs, or env-specific config.
*.bat
*.cmd
*.ps1.local

# === Asset Source Packs (kept locally, only curated files copied into assets/) ===
# Ark Pixel font full-language pack is 158 MB — only 2 TTFs are actually used (in assets/font/).
/font/

# === Local scratch & un-integrated sources (not shipped; keep out of git history) ===
# gitignore 不支持行内注释，pattern 必须单独成行。
# /ref/ = 参考图本地看；/res/ = 临时草稿(cloud.png)；vfx/ = 24M 未集成 VFX 素材库
# *.psd/*.docx = 美术/文档源文件
/ref/
/res/
/assets/sprites/NewAssets/vfx/
*.psd
*.docx

# === Godot Plugins / Addons (installed via AssetLib by each developer) ===
# GUT (Godot Unit Test) is installed locally via AssetLib to keep third-party
# code out of git history. New devs: re-install via Project → AssetLib → search "Gut".
/addons/gut/

# === Local sim / tooling scratch outputs (per-run, regenerated; never shipped) ===
# run_sim.gd 写出的临时平衡/烟雾测试目录（out_btest / out_smoke14 / out_upgrade_ab / out_planab …）。
/tools/sim/out_*/

# === Claude Code Local ===
.claude/settings.local.json
.claude/docs/UPGRADE-PLAN.md
.claude/projects/
CLAUDE.local.md
production/session-logs/
production/session-state/*.md

# === Editor workspace / 本机元数据（per-machine·不入库·2026-07-02 C9）===
*.code-workspace
.ghlp-metadata.json

# === Internal Planning (not for public) ===
docs/IMPROVEMENTS-PROPOSAL.md
docs/MULTI-STAGE-DOCUMENT-WORKFLOW.md

# === Build Output ===
build/
builds/
bin/
obj/
out/
dist/
*.exe
*.dll
*.so
*.dylib
*.app

# === Engine: Godot ===
.godot/
*.translation
export_presets.cfg
# Uncomment if using Godot:
# *.import

# === Compiled Shaders ===
*.spv
*.cso
*.dxbc

# === Temporary and Cache ===
*.tmp
*.temp
*.cache
*.log
__pycache__/
*.pyc
node_modules/
.npm/

# === Archives ===
*.zip
*.tar.gz
*.rar
*.7z

# === Secrets and Environment ===
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json
secrets.json

# === Large Binary Assets (use Git LFS instead) ===
# Uncomment and configure with Git LFS for your project:
# *.png
# *.jpg
# *.jpeg
# *.wav
# *.mp3
# *.ogg
# *.fbx
# *.blend
# *.psd
# *.tga
# *.exr
# *.hdr

# === OS Generated ===
ehthumbs.db
ehthumbs_vista.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk
