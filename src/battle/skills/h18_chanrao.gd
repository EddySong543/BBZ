extends HeroSkill

## h18 相柳【困于泽国】被动 · 状态 · HP4（脆·锁链位）
## 相柳【出战·存活】时，对手【无法主动切换】（含星日免费切换）——被死死缠住、逃不掉。
##   死亡换人 / 紫火调虎离山 / 道具强制切换 等"被动·触发"切换不受影响（只锁对手【主动】切换）。
## 引擎在 can_afford(SWITCH) + is_free_switch_target 统一 gate（_can_switch）；下场即解。
##
## 设计依据（heroes-redesign / build-design-framework）：
##   维度 = 状态（持续束缚·跨回合·填 dark batch 最后空缺；触发=aura、避开 on-hit 同质）。
##   为何宽 combo（trap 引擎）：把对手核心【钉死逃不掉】→ 你全队的铺设（龙破甲 / 暗虎压 /
##     暗龙逼战 / 集火 / 毒）全砸在同一个跑不了的目标上；对手最想做的"换人躲毒 / 躲集火 /
##     换 counter"被锁死。每个"想让敌人留在原地"的 combo 都受益（乘算放大集火与 setup）。
##   agency / yomi：你拿脆皮蛇顶前面锁住对手核心、集火收割；对手只能先点掉蛇(HP4·脆)解套，
##     或预判被缠提前换位 / 留净化手段。
##   §4.4：纯 aura、零伤害；强度由「蛇必须出战暴露在前·HP4 脆·可被秒掉解套」自限。
##     只锁【主动】切换、不锁死亡换人/强制切换 → 不破"出战死了必须补位"的核心规则。
##   与光翼火 h06【淬毒】(状态·叠毒引爆) 机制全异（无镜维度铁律·自由设计）；
##     与娄金(罚切换·真伤)/暗龙(逼战·逼攻) 也不同——这是【硬锁】对手主动切换。
##
## 【实现注记】aura 作用于【对手】、且要 gate 对手的切换动作 → 由 BattleCore 统一收口
##   （本组件只声明 locks_enemy_switch()=true·无状态）。

func locks_enemy_switch() -> bool:
	return true
