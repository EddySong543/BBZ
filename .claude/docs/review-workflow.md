# 审查工作流（Review Workflow）

1. 代码变更（Code Changes）需由相关部门主管代理审查
2. 设计变更需 `game-designer` 和 `creative-director` 签字确认（Sign-off）
3. 架构变更需 `technical-director` 签字确认
4. 跨域变更（Cross-domain Changes）需 `producer` 签字确认
