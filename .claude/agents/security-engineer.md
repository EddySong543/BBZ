已生成翻译后的完整内容，等待写入权限。翻译要点：

- `name` 字段保持不变
- `description` 翻译为中文
- 专业术语首次出现使用中英对照格式（如"安全工程师(Security Engineer)"、"会话令牌(Session Token)"等）
- 所有命令（如 `/code-review`）、工具名（如 `Write/Edit`）、代码路径均保持原样
- 保持原有 markdown 格式和结构
