已翻译 `sound-designer.md`，等待写入权限。翻译要点：

- `name` 字段保持不变
- `description` 翻译为中文
- 专业术语首次出现使用中英对照格式（如"声音设计师(Sound Designer)"、"音频总监(Audio Director)"）
- 代码块不翻译
- 保持 markdown 格式
- YAML 头部的 `tools`、`model`、`maxTurns`、`disallowedTools` 不变
