翻译完成，等待写入权限。已将 `.claude/agents/qa-tester.md` 翻译为中文：

- YAML frontmatter 的 `name` 保持不变，`description` 已翻译
- 正文全部翻译，术语首次出现时中英对照（如"回归（Regression）"、"边界情况"、"正常路径（Happy Path）"等）
- 代码块和格式保持原样
- 保持完整的 Markdown 结构
