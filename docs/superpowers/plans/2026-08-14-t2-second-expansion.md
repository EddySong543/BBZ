# 第二批稀有道具增补实施计划

**目标：** 将 7 件已批准 T2 一次性接入正式目录、结算、UI、联机、AI、测试和文档。

1. 先写核心、目标选择、联机视角与目录失败测试。
2. 添加 7 个无状态 ItemEffect 脚本，并在 BattleCore 接入敌槽目标、借印、改向、强制登场、归营治疗和信息隐藏。
3. 扩展 BattleScreen 的敌方道具槽选择与友方后排选择，复用现有 `item_slot_targets` 协议。
4. 给 BattleAI 增加目标选择和防浪费规则；同步评估／模拟可读参数。
5. 更新 ItemCatalog、正式与审批文档、i18n、映射和占位图标。
6. 运行 Godot Import、定向 GUT、全量 GUT、1920×1080 目标选择／隐藏显示探针及 `diff --check`；不提交。
