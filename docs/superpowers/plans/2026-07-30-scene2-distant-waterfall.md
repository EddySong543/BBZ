# Scene2 远景瀑布实施计划

1. 记录并保护 Scene2 当前未提交参数。
2. 在 `scene2.tscn` 中创建独立远景瀑布材质。
3. 将远景瀑布节点放在中山之后、近景山体与桃树之前。
4. 用低速、低对比、低纹理密度和小视差建立远景感。
5. 通过 `tools/run_godot.ps1` 运行 Scene2 截图探针，按画面只做必要调优。
6. 运行场景加载/回归检查，并复核最终 diff 未覆盖其他改动。
