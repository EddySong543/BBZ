# h07 每回合一次与伪 combo 文案修正计划

**目标：** 将千里自在风的免费切换严格限制为每方每回合一次，并清除 h11/h21 并不存在的同时触发关系；h23 仅做候选机制设计，不实装。

## 任务一：锁定 h07 规则

- 在 `tests/unit/battle/v4/test_heroes_zodiac_v4.gd` 增加同回合第二次免费切换失败、下一回合恢复的测试。
- 在 `BattleCore` 增加可快照的团队级“本回合已免费切换”状态，统一约束 UI、AI 克隆和权威端回放。
- 将联机协议单回合免费切换序列上限收紧为 1，并更新协议、房间、克隆及快照测试。
- 更新 h07 资源、国际化文本、技能注释与设计文档中的旧“不限次”表述。

## 任务二：清除 h11/h21 伪 combo

- h11 文案明确为“娄金出战时”监听敌方下场。
- 删除 h21 会触发替补席 h11 的说明；保留两者各自现有机制，不改结算。
- 同步资源、国际化文本、组件注释和设计文档。

## 任务三：验证

- 运行相关 GUT 单测与 Godot 导入。
- 运行完整 GUT 套件，记录任何与本次无关的既有失败。
- 检查工作树，确认只改动本任务范围且未覆盖用户已有改动。
