# 战斗拖拽指令队列实施计划

> **For agentic workers:** 本计划在当前会话内直接执行；严格按 RED-GREEN-REFACTOR 验证，不创建额外任务或工作树。

**目标：** 将所有会生成本回合战斗指令的控件统一为“拖入顺序栏末端空槽并松开后入队”，由底部动作栏上方的圆角方形槽显示真实权威结算顺序；“结束”继续点击并一次提交整段序列。

**约束：** 保留现有盲选、目标选择、联机权威校验、AI、录像/快照、连环鼓与 H07 免费切换规则；不改背包、测试、设置等工具按钮，不使用截图验证。

## 任务 2B：方案 B「四角卡榫」视觉收口

**文件：**

- 新建：`src/ui/components/command_sequence_slot_skin.gd`
- 修改：`src/ui/battle_screen.gd`
- 修改：`tests/unit/ui/test_battle_command_drag_queue.gd`

**接口：**

- `CommandSequenceSlotSkin.configure(is_empty: bool)`：配置空槽/已填槽的极淡中央底色与四角状态。
- `CommandSequenceSlotSkin.set_hot(value: bool)`：只预收紧、提亮四个卡榫，不缩放整个槽位。
- `CommandSequenceSlotSkin.lock_progress: float`：驱动四角从外侧向内容合拢。
- `CommandSequenceSlotSkin.flash_strength: float`：驱动合拢瞬间的短促暖光，并记录测试可读峰值。
- `CommandSequenceSlotSkin.debug_geometry()`：向无截图测试暴露八段卡榫、中央底色和颜色状态。
- `BattleScreen._play_command_slot_land(slot: Control)`：播放约 0.20 秒“四角合拢—暖光锁定”的落位反馈。

- [x] 把方案 A 的阶梯外框测试改为“无完整底板、四个像素护角、极淡中央底色”的失败测试。
- [x] 增加热区不缩放整格与四角合拢、暖光峰值、内容显现测试，确认方案 A 无法通过。
- [x] 用自绘 `Control` 实现 58×58 四角卡榫；所有矩形保持整数坐标且禁用抗锯齿。
- [x] 保持空槽 `0.48`、热空槽 `0.82`、已填槽 `1.0`，仅替换方案 A 的完整轮廓和凹面。
- [x] 合法拖入后播放卡榫由外向内锁定、短闪暖光和内容由淡到实的反馈；道具、行动、切换头像与撤销键保持原层级。
- [x] 运行聚焦 GUT、完整 `tests/unit/ui` 与 `git diff --check`，确认不回归结算降透明度和单行居中。

## 任务 1：统一有序指令契约

- 新增纯数据的回合指令队列，统一表达行动、切换、主动技、道具及其目标/变体。
- 升级网络提交与服务端克隆预检，使整段按顺序原子校验与重放；旧 payload 保持兼容。
- 为“道具→行动”和“行动→道具”的序列化、拒绝非法步骤、双方左对齐补测试。

## 任务 2C：黑色角套与运行态一致性修正

**文件：**

- 修改：`src/ui/components/command_sequence_slot_skin.gd`
- 修改：`src/ui/battle_screen.gd`
- 修改：`tests/unit/ui/test_battle_command_drag_queue.gd`

- [x] 将四角卡榫改为说明框角套同族的近黑像素块，彻底移除中央底色；落槽反馈改为短促墨色落印。
- [x] 撤销 `x` 去掉暖色按钮底板，只保留透明点击区与黑色像素字形。
- [x] 修复空队列在 F6 开场、结算、换人与技能演出时被设为不可见的问题；始终保留顺序条并与底部按钮同步 `1.0 / 0.4` 透明度。
- [x] 将切换静态标记扩大到头像边长的约 `42%`，固定填入右下角且不与右上撤销区重叠。
- [x] 拆分道具拖动与落槽表现：拖动虚影只显示 icon；落槽后恢复 `ItemFrameStyle` 同源的稀有度底色、图案阴影、完整外框和 icon。
- [x] 聚焦回归覆盖空队列播放态、黑色镂空卡榫、透明撤销键、切换标记最小可读尺寸及道具双形态。

## 任务 2D：卡榫可读性、刷新缓动与切换条目统一

- [x] 保持中央镂空，将纯黑卡榫改为战斗按钮同族的“深暖褐压边＋旧金属米金亮面”；撤销 `x` 同步使用双色像素层。
- [x] 新增 `0.12s` 的 `2px` 下沉归位＋轻淡入刷新反馈，只在队列内容签名改变时播放，普通状态刷新不重复触发。
- [x] 切换拖动阶段继续显示目标头像；落入顺序条后改为与基础行动同尺寸逻辑的纯 `SwitchIcon`，目标英雄仍由队列数据保存。
- [x] 聚焦测试覆盖卡榫亮度层级、刷新动画单次触发及切换条目不含头像框。

## 任务 2E：加粗倒计时米色角套与完整切换按钮

- [x] 角套主体由 `4px` 加粗至 `6px`，外角切去 `2×2px` 形成像素阶梯圆角，中央仍保持镂空。
- [x] 四个向内位置各增加一个 `2×2px` 小铆点，只提供轻量节奏，不连接成第二层边框。
- [x] 角套亮面严格改用普通倒计时的 `#F2E8CC`，暗衬复用倒计时装饰的深色层级；撤销 `x` 同步更新。
- [x] 顺序条中的切换条目直接复制正式切换按钮的蓝色果冻底、像素外框和 `SwitchIcon`，不再只显示裸图标。

## 任务 2：拖放输入与顺序条

- 为动作按钮、主动技/分支、切换候选和道具槽建立同一拖拽源协议。
- 删除中央大释放框。顺序栏始终在现有步骤末端提供一个圆角方形空槽；只有该槽接受松手。
- 拖动实体复用来源控件的美术：基础行动/主动技能/变体保留按钮底、图案与消耗角标；道具只保留内部图案，不复制底色、阴影与道具框；切换候选保留英雄头像框，并在头像右上角叠加静态切换图标；禁止以文字标签充当拖动实体。
- 拖动开始后来源控件压暗；接近空槽时拖动实体向槽心吸附并略微缩小，合法松手后缩入空槽并播放一次落印反馈，非法松手则回弹到来源。
- 在五个底部动作按钮上方建立动态居中的圆角方形槽列，色板与背包按钮一致并留出安全间距。已编入步骤显示对应美术，右上角提供同色板像素 `x` 用于撤销该步；末端只保留一个下一步空槽，不使用文字条目、分隔箭头或换行。
- 点击底部动作按钮只进入旧版预选态并展开龙御极等二级按钮，不确认指令；拖动最终按钮进入顺序槽才确认。切换按钮仍以点击打开候选层，实际头像必须拖入顺序槽才生成切换步骤；“结束”继续点击提交。

## 任务 3：现有特殊流程接入

- 将需目标、需候选、H07 免费切换、H24 减费变体、龙御极/暗潮、连环鼓第二行动接到入队完成点。
- 本地预览按已排步骤重放，保证后续按钮可用性读取的是队列中此前步骤造成的状态。
- AI 仍可生成相同契约；联机、超时默认、录像/快照沿同一入口。

## 任务 4：回归与运行时验收

- 覆盖拖拽未松手不入队、只有末端空槽接受松手、合法松手只入队一次、非法区域回弹、结束整体提交、顺序槽与 payload 一致。
- 断言中央大释放框已移除、空槽不遮挡底部按钮与结束按钮、队列增长后仍保持单行居中。
- 运行相关 GUT，再运行完整测试；用 Godot 日志与几何/输入断言验证吸附中心、顺序槽和拖动实体，不启动或关闭用户编辑器。
