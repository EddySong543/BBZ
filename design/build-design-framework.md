# 波波攒 — Build / 经济 / 连携 设计纲领

> **当前路线覆盖（2026-08-31）：**连携、通用性与五维平衡继续作为道具设计公理；“主动免费三选一 → 取得后锁定本回合 → 使用时逐件付费”已经是 PvE / PvP 通用规则，不再审批。公平匹配和对人专项反制不属于当前 PvE 验收项。
> **性质**：跨系统的**设计公理与方法论**，不是单一机制的 GDD（故不放 `design/gdd/`、不套 8 段模板）。
> **来源**：2026-06-14 Eddy 与 Claude 的三段地基讨论，逐条对齐后的双方共识。
> **地位**：道具、英雄、体系的所有具体设计都应先拿本文（尤其 §6 主定理）过一遍。
> **配套 memory**：[[synergy-master-theorem]] / [[build-structure-shallow-wide]] / [[item-system-skeleton]]。

---

## 0. 一句话定位

策略 = **资源的分配**。本作砍掉了重赛前卡组构筑，换成轻量的局内 draft；丢失的多样性由**道具系统**与**连携**接回。游戏好玩的核心 = **非固定套路化的对局**——靠英雄 + 道具的搭配选择产生变化，单人、PvP 皆然。

---

## 1. 资源框架（三层 + 动作槽）

资源分两种形态：

- **流量资源（唯一稀缺）= 动作槽**：每回合仅一个动作，不可囤积。这是策略的核心稀缺，也是一切"超模"的判据来源。
- **存量资源**：能量、血量、道具（可囤、可跨回合）。

战斗资源分三层（上游 → 下游；这是效果分析，不是英雄主定位）：

| 层 | 内容 | 说明 |
|----|------|------|
| **决胜层** | 进攻端 / 防御端 | 直接决定胜负 |
| **燃料层** | 能量 | 攻防之间的**选择权 / 元货币**，地位高于一般增益（不是普通"非决胜维度"） |
| **增益层** | 节奏(Tempo) / 状态持续 / 信息(Yomi) / 容错 / 干扰 等 | 通过放大决胜层间接取胜 |

- **运转端（能量 / 动作槽）= 放大决胜维度强度的"系数"**。
- **动作槽 = 水桶的第二块板**：未来可能推出"+1 动作槽"设计——但见 §4，它是全游戏最高级超模。
- 一切设计最终都落到两个轴：**强度**（对应攻防）× **容错率**（对应对不良事件的容纳）。效果可跨多层、也可"降一层换另一层"。

---

## 2. 道具系统骨架

> 🔴 **2026-08-30 当前标准（Eddy 拍板）**：完整现行规则以 [`2026-08-30-item-system-current-standard.md`](../docs/superpowers/specs/2026-08-30-item-system-current-standard.md) 为唯一入口；道具同时按**单次机制价值、逐次使用费、耐久、占格、金币价格**审核。
> PvP 的开局、同步抽取、候选、框位、断线与实例语义已经在 [`2026-08-30-pvp-item-acquisition-instance-boundaries.md`](../docs/superpowers/specs/2026-08-30-pvp-item-acquisition-instance-boundaries.md) 封板；五维定义、公式与战后生命周期见 [`2026-08-30-item-four-axis-balance-design.md`](../docs/superpowers/specs/2026-08-30-item-four-axis-balance-design.md)。后者为避免断链保留旧文件名，正文已经升级为五维。
> 此前固定 1 能抽取、使用免费、抽后即用、自动抽取、固定目录估值、按稀有度固定耐久或形状、低金币标尺、使用即销毁、强制升级/图纸进 T3 均已退出目标设计；当前代码尚未全部迁移。

- **分级（用 T1/T2/T3 称呼）**：
  - **T1** = 机制价值通常在 1–2；简洁、稳定并可独立使用。
  - **T2** = 机制价值通常高于 2，不设固定中心或上限；连接两个以上公共玩法轴并改变一次计划。
  - **T3** = 机制价值通常高于 3，并且必须相对 T1/T2 产生质变，而不是单纯放大数值。
- **使用规则**：
  - **不占动作槽**（这是道具超模的根源——任何免动作的正收益都天然超模）。
  - **每件逐次支付使用费**：费用范围 `0–4` 能，大部分为 `1–2`；一回合可使用多个不同实体，但**同一实例每回合最多一次**，多耐久件每次都重新付费。
  - **使用时点 = 揭示前**（3A 定）：道具随动作一起在盲选阶段提交 → 道具是**主动计划件**，不是揭示后的反制件；干扰 / 救场 e 因此是"**预判**"而非"反应"。
  - 每次权威使用扣 1 耐久；归零才销毁，未归零实例继续保留。物品耐久与它产生的持续效果次数分开记录，具体战内位置由新 PvP 方案决定。
- **槽位与获取**：
  - T1/T2/T3 成品都能作为真实资产进入战备方案；图纸或升级只能是具体内容，不能成为 T3 的通用门槛。
  - PvP 每回合至多一次手动免费抽取；至少一个公开框为空时，从真实背包随机 3 选 1 或放弃。候选不刷新，选中件公开锁定本回合，下回合可用；框满不能抽。
  - 战末剩余实例按原归属退回背包或保险格，耐久跨当前 Run 保留，搜刮在退包后进行。
- **超模闸门**：单次价值范围、取得规则、公开信息、实例每回合限用、耐久、占格、金币资产和对手反制共同承担；具体取得规则必须简单、可读，不能只靠价格解释单场失衡。
- **PvP 道具来源 = 掉落 / 养成（非对称），靠调爆率平衡，作为粘性钩子**——这是 Eddy 明确拍板的取舍（修订了"绝不卖强度"那条，见 [[online-pvp-and-monetization-direction]]），不再讨论。
- 对外统称“**道具**”；“遗物”只是适合承载持续规则的物理形态，不另建抽取系统，也不自动等于 3 耐久。

> 备注：本骨架取代了旧的"药水=带3瓶/首版不碰能量"设想（[[potion-system-concept]] 已标记被取代）。

---

## 道具系统详设（B1，2026-06-14）

### 变强路径（D1=A）
- **不设通用升级或合成梯子**：三种稀有度成品都可直接构筑、抽取和使用。
- 个别道具、事件或商店以后可以做转化/铸造，但必须按具体文本定价，不能要求玩家记住一套隐藏配方，也不能把它当成 T3 的唯一来源。

### 道具的标签与设计取向（D2）
- 旧运行时道具仍带一项**旧六维技术标签**，只服务尚未迁移的池加权；新真实背包抽取不再依赖它。后续机制审查可以保留元件角色（导出 / 干扰 / 泛连携 / 填隙 / 自成核 / 回路）作为设计分析，不作为玩家主定位。
- **设计取向**：道具 = 游戏的"**多而广**"内容层——**每张一句话能看懂，但总量很多、变化丰富**（类炉石 / 尖塔单卡）。英雄少而干净，**道具多而花样**（minor / 连接 / 点缀都在这一层）。

### 池子模型（D2，与共通 5 地板同构）
- 道具来源是玩家赛前装入的**真实背包实例**，不是全局 T1 地板池；PvP 候选从当前仍在背包中的合法实例随机生成。
- 系统临时保底池只在连续 Run 的战备数量不足时补位，不混入真实资产；临时件正常显示同名估值但不可兑现。

### 获取与槽位（D3·2026-08-30 重置）
- 赛前战备方案保存英雄、真实道具实例、固定形状与摆放；同名件上限继续单独校准。
- 双方以 3 个空公开框开局。每回合行动规划前先经过独立同步抽取门；有空框且有合法实例时，可手动免费抽取一次或放弃，机会不累计。
- 候选从真实战斗背包按 UID 无放回展示 `min(3, N)` 件；同名不同 UID 可以同时出现，不足 3 件不补假道具。关闭重开不能刷新。
- 候选私密，双方最终取得结果在同一权威节拍公开；选中实例锁定本回合，第 `R+1` 回合才可用，然后双方才开始完整行动规划。
- 耐久归零的框在本回合最终清理时腾空；抽取超时放弃，行动超时提交“攒”，断线不停钟并暂留席位 30 秒。交换、复制与临时件按 UID 实例专项规则处理。
- 现行 `STARTER_ITEM_IDS`、自动解锁、地板池加权与升级抽取属于尚未迁移的旧运行时，不再作为目标设计。

### 道具公开：锁定一回合

“新取得道具本回合即可使用”已被否决。第 `R` 回合取得的道具公开后保持锁定，第 `R+1` 回合规划阶段才可投入有序序列，保证对手拥有一个完整公共信息窗口。

### 回合流程 = 玩家编排的有序序列（D4，含「顺序」机制）
**核心新增（Eddy 2026-06-14）**：道具在动作**前 / 后**使用结果不同，**顺序由玩家编排**。
- 一回合提交 = 一段**有序序列** `[道具…, 动作, 道具…]`——玩家决定哪些道具在动作前、哪些在动作后，连同动作**揭示前盲选提交**（3A）。
- 这让"道具铺垫 → 动作收割"或"动作 → 道具追结算"成为玩家可控的**回合内微 combo / 链条**（接 §5/§6）。
- **操作定案（Eddy 2026-08-28）**：客户端本地记录实际施放顺序；按住左键拖出只预览，未松手不算使用；在合法区域松开左键才视为释放、播放本地动画并追加待提交步骤。最终确认时一次提交完整序列，确认前不向对手逐条公开。松手后的撤销/重排等 UI 细节另议。
- **内容护栏（Eddy 2026-08-28）**：每件道具只描述自己在结算时改变的状态，后续步骤自然读取新状态；不用“若在攻击前/后”硬造分支，也不把“一个顺序必亏、另一个必赚”当成顺序博弈。效果默认不追溯此前步骤，玩家文案不补“此后”。
- **结算模型（✅ 2026-06-14 确认）**：
  - **己方序列严格按编排顺序逐步结算**——自身 buff / 防御（甲盾）/ 读自己动作结果 的道具按序生效（故"动作前 +攻" ≠ "动作后追伤"）。
  - **双方序列从左侧对齐，逐列结算**：每列读取双方当前待执行步骤；一方序列先结束时该侧为空，另一方继续。
  - **同列均完成，不设先后手**：同列两方已经开始的步骤都有权完成，服务器内部遍历顺序不得制造 P1 / P2 座位优势；因此同列对攻仍可互相命中，也可同时死亡。
  - **列间效果可以延后下一步骤**：服务器可在某方下一真实步骤前生成等待，但客户端不能提交等待，也不改变剩余步骤的原顺序。完整规则见 [`2026-08-30-h03-sequence-shift-design.md`](../docs/superpowers/specs/2026-08-30-h03-sequence-shift-design.md)。
  - 本回合免费抽取不进入可复制的道具步骤；新取得件因锁定也不能加入当回合序列。
  - 平局如何处理（重赛 / 算分）= 后续 PvP 细节，不影响模型。
- **联机边界**：本地松手动画只是私有编排反馈；服务器对最终序列做权威重放，随后广播事件流 / 快照驱动双方公开动画。双方左对齐逐列调度器与当前「连环鼓」后续行动已经落地；但现有 UI 与 `{action, item_slots}` 协议尚不能提交任意道具 / 行动统一排序，仍需迁移 AI、协议、回放 / 快照和 UI。详见 [`2026-08-28-item-sequence-interaction-design.md`](../docs/superpowers/specs/2026-08-28-item-sequence-interaction-design.md) 与 [`2026-08-30-h03-sequence-shift-design.md`](../docs/superpowers/specs/2026-08-30-h03-sequence-shift-design.md)。

### 目标规则（D5=A）
- **默认自动指向**：伤 → 敌方出战 / 甲盾 → 己方出战 / debuff → 敌方出战。
- 个别特殊道具可带**特殊指向**（指定后排 / 全体等），个案开放。

### 能量预算（🔴 2026-08-30 当前规则）
- PvP 每回合的标准三选一**免费**，不消耗能量；代价由每回合一次机会、空框、公开锁定、真实实例和放弃不重抽承担。
- 每件道具拥有 `0–4` 能的逐次使用费，大部分位于 `1–2`；多耐久件每次使用都重新支付。`3–4` 能允许用于能改变局势的整轮计划件。
- 服务器逐步骤检查并扣费：前件产生的能量可以支付后件，后件不能倒付前件。费用与 T1/T2/T3 不机械绑定。

---

## 3. build 结构：浅而广，一个明确贡献 + 其余不塌

**核心问题**：与"少角色、build 绑定角色"的游戏不同（那类游戏里一个角色的各种体系各覆盖 1-2 维），本作每个英雄 = **共通 5 能力 + 1 差异化主/被动**，build = **3 英雄 unique + 最多 3 道具 ≈ 6 个元件**。

**答案（结构性）**：

1. **共通 5 = 永远在线的基础地板**。所以英雄不必覆盖所有方向，而应有**一个清楚的主要贡献，其余不塌**——"不塌"由共通 5 免费保证；设计张力上移到 unique 是否真正改变了玩家的一项基础决策。
2. **共通 5 = 通用对接口**，这一条同时撑起两件事：
   - **去流派化**之所以本作做得到、尖塔做不到：每个英雄共享同一套基础操作 = 通用插槽，任何 unique / 道具都能插进任何 build 而地板不变（尖塔的卡有颜色/角色锁，没这层地板）。
   - **"核心锁死在波波攒"**：基础 5 是地板，unique / 道具是地板上的改装件、**永不替换地板**。道具一旦强到替换基础操作，通用地板崩 → 去流派化与"核心在波波攒"一起垮。这是"不让游戏引向道具层"的**结构性理由**；2026-08-30 后由逐次使用费、公开锁定、公开框、实例限用、耐久、占格、金币与反制共同执行。
3. **build 身份 = 浅而广**（对比角色绑定游戏的"深而窄"）：每个 build 就 6 元件的薄拼装，但英雄池大 + 道具多 → 组合空间爆炸；丰富度来自**组合广度**而非单 build 深度。
4. **代价**：6 元件每个占 ~17%，**没有容错冗余，一个废件即灾难** → 每个元件要么自身高影响、要么强联动，**不能有尖塔式凑数小料**。

详见 [[build-structure-shallow-wide]]。

---

## 4. 标准值（A1，2026-06-14 定）

### 4.1 五项基础操作（标准值之锚；大波→3、大防留 2）

| 操作 | 能耗 | 效果 | 端 |
|---|---|---|---|
| 攒 CHARGE | 0（占动作） | +1 能 | 能量 |
| 波 ATTACK | 1 | 1.0 HP 伤 | 进攻 |
| 防 DEFEND | 0（占动作） | 挡"波"（仅挡基础攻击，不挡大波/穿防） | 防御 |
| 大波 BIG_ATTACK | **3** | 2.0 HP 伤 + 穿"防" | 进攻 |
| 大防 BIG_DEFEND | **2** | 挡全部 | 防御 |

- **切换 SWITCH 单独算**：0 能占动作的调度动作，无攻防数值、可操作空间小，**不计入标准值**。
- **大波=3 的依据**：把"攒→波"循环算持续吞吐——波 = 0.5 HP/动作；大波=2 时 0.67 HP/动作（严格优于波 = 失衡），大波=3 时回到 0.5 HP/动作与波打平 → 大波回归"纯爆发 / 穿防"定位。
- **大防=2（Eddy 选 B）**：攻防对称被打破，防御比进攻便宜。⚠ 已知风险 = 偏苟 / 可能拖长对局（与 1-3 分钟目标反向），**接受并留待试玩观察**。
- **代码现状（2026-06-16 已实装·B/A2）**：`action_def.gd` 大波 = 6 半能（3 能）、大防 = 4 半能（2 能）；能量走半能制。✅ 已是目标态（此前此注记误记为"大波 2 能·待 D 阶段"，2026-06-18 校正）。

### 4.2 两条汇率（设计任何效果时的尺子）

| 汇率 | 值 | 来源 / 含义 |
|---|---|---|
| **占动作的操作** | 1 能 = **1.0 HP** | 波 = 1 能 / 1.0 HP |
| **T1 道具（不占动作）** | 机制价值通常在 **1.0–2.0** | 取得成本尚待 PvP 专项；不能把能量、HP、护甲和规则改写机械换算 |

道具不再用统一“半价”换算。稳定值只是起点；条件收益必须由读心、规划或风险支付，并按“三份同类 + 本回合行动”的最坏组合验算。

### 4.3 标准值表

**进攻 / 防御端（占动作，1 能=1.0 HP）：**
| 费用 | 伤害基准 | 防御基准 |
|---|---|---|
| 0 | —（攒走能量端） | 挡波（条件性，=防） |
| 1 | 1.0 HP（=波） | — |
| 2 | 2.0 HP | 挡全部（=大防） |
| 3 | 2.0 HP + 穿防（=大波，含 +1 费 tempo/穿防溢价） | — |

**道具端（不占动作；2026-08-30 五维基准）：**
| 稀有度 | 单次价值审查尺 | 基准职责 |
|---|---:|---|
| T1 | 通常 1.0–2.0 | 简洁、稳定并可独立使用；核心件通常高于 1.0 |
| T2 | 通常 >2.0；无固定中心或上限 | 连接两个以上公共玩法轴，改变一次计划，不是 T1 数值翻倍 |
| T3 | 通常 >3.0，且必须质变 | 相对 T1/T2 产生质变，成为一轮计划或 build-around |

单次机制值之外，必须再独立审核逐次使用费、金币、耐久与占格；完整公式见五维平衡基准。

### 4.4 偏移规则

- **超标** → 必须带负面效果或释放条件（如"血量 < x"）。
- **低标** → 必须好联动（轻 build、可选项少，故低标设计要**放宽**）。
- **T3 质变审查独立于粗数值**：即使粗收益很高，没有改变计划的纯加法效果仍不够 T3。
- **灵活性税**：跨功能元件（同时攻+防）在同费用下天然强于单功能元件（第一段「灵活性溢价」的复发）。修法：各功能都压到单功能基准以下，或单独抬费。
- **能量道具特例**：+1 能不仅是直接收益，还扩大后续选择；必须按实际结算顺序扣除本物使用费，再检查无损回本、循环、为后件融资与多耐久重复兑现，不能只按伤害等量书写。
- **铁律：不许任何操作无条件压制另一个操作**（否则玩家天然产出强度表，"抓不到强卡没法玩"）。道具是核心系统，但同样不许全方位碾压。

### 4.5 数值地基（A2，2026-06-14）

> 🔴🔴 **2026-07-03 再修订（Eddy 执行 A·`PASSIVE_ENERGY_GAIN=2` 恢复）：被动 +1 能/回合已恢复 = 下方 A2 原文重新生效。**
> **实证**：2026-06-24 去被动后从未跑过 sim；本日 2×2 对照（新/旧道具经济 × v1/v2 评估·各 20 局）全部 85-95% 打满 120 回合上限、"防"占 72-79%——攒-only 下攒是唯一收入且露破绽、防免费挡一切 → **最优解=互相龟死**（当年反龟意图在最优玩法下反向造出终极龟）。被动 +1 加回 → 85% 分出胜负、均值 71 回合、波 37%、道具用量 3.7×（`tools/sim/out_econ_passive_test`）。
> **观察项（Eddy 点名）**：被动恢复后「攒」作为基础动作的重要度勿归零——攒的定位 = 爆发加速器（蓄大波/大防/养道具），sim 持续盯攒使用率。
> **连带遗留**：h17 已在 6-24 从【逼战】改【镇压·沉默】，不随被动恢复回改（沉默设计自立、与被动无关）。

> ⛔（溯源）2026-06-24 修订原文：去被动 +1、能量收入仅靠攒、攒回归核心反龟手段——**已被上方 2026-07-03 修订推翻**。

**核心问题**：攒-only 模型把进攻吞吐锁死在 **0.5 HP/回合**（每打一次波都得先攒一回合换能量）→ 任何 HP 下都达不到 1-3 分钟节奏（这正是现状对局 5-10 分钟的根因）。

**决议**：
- **能量收入 = 被动 +1 能/回合**（上一回合最终结算时到账，供本回合使用；第 1 回合不额外发放）；**攒动作额外再 +1**（在行动步骤即时到账，可支付后置道具；攒 = **爆发加速器**，不再是每回合必做的税）。→ 基本每回合都能波（~1.0 HP/回合），攒留给蓄大波 / 道具。完整费用与序列预算见 [`2026-08-30-item-energy-retrieval-baseline.md`](../docs/superpowers/specs/2026-08-30-item-energy-retrieval-baseline.md)。
- **HP 范围保持 3-7**（脆 3-4 / 标准 5-6 / 坦克 7，avg 5）。
- **节奏估算**：3 英雄 × ~5 HP，~1.0-1.5 HP/回合（含道具行动外伤害）→ **~12-20 回合 ≈ 1.5-2.5 分钟**，落进 1-3 分钟目标。**被动能量量 = 首要调速旋钮**，试玩再微调。

**「波波攒」identity 说明**：被动 +1 让攒从"强制税"变"主动加速"——大多数回合真能波（爽点），攒为大招 / 道具蓄力。重心从攒偏向波，但攒经"大波 / 道具需求"保持相关。

**不变项**：初始能量 1、默认 MAX_ENERGY 10、波 1 能 / 1.0 HP（A1 锚）、大波 3、大防 2、切换 0 能占动作。技能可改变动态上限，但不得反向删除已经持有的超额能量。

**代码现状（2026-07-03 更新）**：大波 3 能 + 半能制已落地；**被动 +1 能已恢复（`PASSIVE_ENERGY_GAIN=2`·见本节顶部 🔴🔴 修订）**。能量收入 = 被动 +1 + 攒额外 +1（鼠再额外 +0.5/次获得）。

### 4.6 防御铁则 + 新机制（2026-06-16 定死）

🔒 **二元铁则：力量 = WHETHER（能不能），不是 HOW MUCH（多少）。** 完整正文见 `design/heroes-redesign.md` 顶部铁则块；要点：
- 🔓 **护甲解禁（2026-06-22 Eddy）**：临时护甲 / 护甲（额外血量层·吸收落地伤）现为**合法设计工具**（当时首用 = 牛金 h02【山岳为屏】；该机制已于 2026-07-30 重做退役）；下方"禁临时护甲"原条作废。但 **防 / 大防本身仍是二元整体挡**（挡=归零、不挡=全额），护甲与"防挡不挡"正交、只吸收没挡住落地的伤、真伤无视。
- ~~**禁止临时护甲 / 护甲点数吸收防御模型**~~（**已解禁·见上**）。防 / 大防 = 二元整体挡。
- **分级整体挡**：防 = 挡「波」级整个攻击；大防 = 挡「波 + 大波 / 穿防」= **天花板**。挡 = 归零、不挡 = 全额落地。
- **穿透三档**：穿防（防挡不住、大防能挡，如大波 / 牛金升级后的「波」 / 鸡 2 层）／**穿大防**（连大防都挡不住——**只授"公开慢蓄 / 需铺垫 payoff"**，如鸡满 4 层或高阶一次性道具；禁便宜瞬发；counter = 换核心吃 / 预防）／真伤（无视一切防御 + 护甲，无敌仍 0.5；**英雄仅娄金**——道具可放开，但须过下方「真伤道具 4-gate」）。
- 🆕 **真伤道具 4-gate（2026-06-20 定）**：真伤跳过整套防御博弈（= 波波攒 yomi 核心），故道具用真伤须**同时**满足——①触发轴 ≠「对手切换」（娄金专属）②一次性或重代价（弃命 / 弃全能 / 濒死），**禁常驻反复真伤** ③单次量压 tier 基线（真伤价值在「穿透」不在「数字」，禁用大数字淹护甲）④形象不复制「反逃窜 / 追退场者」。判例：`同归于尽`(阵亡 0.5) / `玉石俱焚`(阵亡 1.0) 四条全过 = 放开；娄金只占「反切换真伤」一格，「可主动引爆 / 处决线 / 真伤 DoT / 当 build 核心」整片留给未来英雄（⚠ 历史补注：「处决线」曾用于 h03 虎，2026-06-15 弃用；双段命中版于 2026-07-30 退役；随后采用的“对攻先击 / 击杀断招”又于 2026-08-30 退役。现行【白额雷音】改为每回合首次基础攻击命中后，令敌方剩余序列先等待一次）。底层 = 判据 A（§14）。
- **打穿大防** = 破防（降防御等级：龙 −1 级）+ 资源剥夺（猴碎能 / 骗防）+ 授权穿大防 payoff。**不靠更大数字淹没护甲。**
- **护甲 = 额外血量层**（兔 +0.5 / 道具）：只吸收"没挡住、落地"的伤、先扣甲后扣血，与"防挡不挡"正交；真伤无视。
- 能量允许 **0.5**。
- **命中边界**：只有「波／大波」穿过防御门才算命中并结算相关效果；护甲吸收仍算命中，独立主动技、追击、反击与独立道具伤害不算。
- **额外触发边界**：额外触发只重复英雄技能；附在攻击上的道具效果每次攻击最多结算一次。
- **多段攻击的 proc 被"必须命中"gate**（整次攻击被挡则不触发；额外段只按英雄技能规则结算，不能借道具附效乘算脱缰）。

🟢 **新机制·轻量治疗**：防御 / 续航向回血（如道具生命药水）——补防御维度"续航 / 熬"轴（原纯掉血无 heal）。后续用 heal 须克制（量小 + 绑防御回合 = 反 stall，勿放大拖慢 1-3 分钟节奏）。

---

## 英雄主定位与旧标签兼容（A3，2026-07-30 重写）

> 本节取代 2026-06-14 的「六维主标签 + 每维 4 人」英雄设计约束。完整裁定表与当前 24 人试归类见 `design/heroes-schools.md` §3。

### 五种唯一主定位

| 主定位 | 主要改变的基础对象 | 一句定义 |
|---|---|---|
| **进攻** | 波 / 大波、敌方 HP | 提高攻击兑现或制造击杀线 |
| **防守** | 防 / 大防、我方 HP | 避免、修复或转移己方损失 |
| **经济** | 攒、我方共享能量池 | 增加或放大我方可支配能量 |
| **调度** | 我方行动槽、动作与切换 | 增加或重排己方行动和部署选择 |
| **控制** | 敌方选招、能量、行动与位置 | 减少、迫使或打乱敌方选择 |

### 主定位规则

- 每名英雄只有 **1 个主定位**；归类发生在机制通过角色幻想、基础交互、combo 与 yomi 检查之后。
- 主定位看玩家反复围绕哪项基础决策出牌，不看触发词、数值奖励或效果载体。
- **节奏 / 状态 / 反击 / 爆发 / 续航**降为机制标签；**combo**是所有英雄的连通度要求，均不再占职位。
- 技能若同等依赖两个职位，通常是过载信号；删掉 rider 或把 minor 下放道具。

### 英雄规模与覆盖

- **首发仍为 24 名**（h01–h24，12 生肖 + 12 暗生肖），但不再规定每类人数相等。
- 0 人代表真实缺口，1 人代表覆盖偏薄，2 人以上即为已成立；这些只是审计警报，不是设计配额。
- 防同质化比较「基础交互 + 触发面 + 产出 / 消费物」，不比较某一职位有几人。
- 初始默认 3 英雄仍应覆盖易懂的攻、防、资源管理教学，但不强制固定为三个旧字段值；待新定位进入 UI 时另行确定初始阵容。
- 英雄解锁仍使用金币（初始 3、快速到 ≥12），与主定位迁移无关。

### 旧六维技术标签

- `HeroData.dimension` 与道具 `dimension` 暂时继续服务 T2 加权抽取，仍使用进攻 / 防御 / 能量 / 节奏 / 状态 / 干扰六个旧字符串。
- `HeroData.team_role` 暂时继续服务 BP 三色框，仍使用经济 / 进攻 / 防守。
- 两类旧字段都是迁移期实现细节，**不得作为新英雄设计输入，也不得维持任何人数配额**。
- 24 人机制稳定后，统一设计 `primary_role` 与受控机制标签，并同步迁移英雄数据、道具匹配、BP UI 和测试；在此之前禁止逐英雄局部改字段。

---

## 5. 连携：从量变到质变

- 单操作被标准值锁死；**多元件组合可超标准值，产生 1+1 > 2**。这是提升 build 的核心钥匙，PvP / PvE 皆然。
- **输入 / 输出**：道具 A 产出输入 → 英雄 A 接收并产出强化后的新效果 = 一次连携。角色不固定，输出可成为下一连携的输入 → **链条**。
- 后续可复用“效果绑定使用者英雄、跨回合保留并支持围绕其规划”的连接结构（如还魂丹），但不设为硬性配额；同一英雄只允许使用一次还魂丹，且它作为最低层保险可取消致命伤害、生命支付、失去生命与规则处决。
- **链条**：每步乘一个系数 → 总系数指数增长，但**越长越不稳**（PvP 要命）。
  - **链条天然自限**：build 仅 ~6 元件 → 链长上限 6、实战 2-3，指数跑不飞（比尖塔安全）。
  - **真正的 PvP 炸弹是回路 e**（自我连携）：它绕开"元件数量"天花板，链长无限 → **PvP 默认禁回路 e、只活 PvE**（2A 定）；**可实验性加入，但必须明确标注哪些元件是回路 e**，供测试时再定是否在 PvP 放开。
- **e（element）** = 角色 / 道具 等的统称。一对 e = 连携；一堆连携 = **体系**。轻 build 下，组成体系的**最低数量要求放宽**。

### §6 主定理（第一公理）：系内加算、系间乘算

> **单体系堆叠走加算**（线性、封顶、受标准值锁）；**跨体系配合走乘算**（放大、1+1>2）；且**任何时候 系间乘算系数 ≥ 系内乘算系数**。

这条是反固化总钥匙，同时解掉：

- **平衡（e3 单卡 = e1+e2 组合 问题）**：单卡走加算、组合走乘算 → 强单卡永远平替不了真组合 → "强单卡贬低组合"被源头禁止。
- **催化剂式排异**（参考其他卡游：体系增强卡的系内 ×系数 > 系间 ×系数 = 排异罪魁）：被本定理直接 ban。
- **配合性应 > 排异性**（暗杀神四流派两两互助为正态）：本定理保证混搭永不吃亏。
- **自洽 §3 浅而广**：广（混）≥ 深（纯）→ build 自然走浅而广。
- **自洽 回路 e 危险**：回路 e = 系内乘算的唯一例外 → 必然是 PvP 炸弹。
- **PvE 指数空间**：PvE 无对称对手，**绝对量**可解封（系内也能乘、可指数）；但**相对关系不变**（系间仍 ≥ 系内）。

详见 [[synergy-master-theorem]]。

---

## 7. 元件类型

### 体系内（引擎零件）
- **输入 e** / **输出 e**：连携的供给与接收端。
- **回路 e**：既是输入又是输出，触发机制后反哺强化该机制，可与自身连携 → 指数放大，**PvP 极危**（见 §6）。
- **导出 e**：把一端导向另一端（如**吸血** = 攻→防回血），补 build 缺失维度、保证整体强度。**PvE 远比 PvP 值钱**（永久掉血让"攻→防"成刚需；PvP 单局血量重置，吸血价值骤降）——是"PvP/PvE 极大区分"在元件层的具体落点。

### 体系外（点缀，可以多一些 —— 游戏真正的丰富内容）
1. **自成核（条件超标 e）**：高天花板、高方差。注意它**不是无连携**——"条件"本身就是隐藏输入，玩家会去 build"满足条件的链"，本质是"以条件为输入的微体系"。
2. **泛连携 e**：自身弱、连通度高。是**去流派化的实际执行者**（把体系彼此粘起来的结缔组织），地位高于"拓宽组合"。
3. **填隙 e**：基准 ±、无联动、无负面、补弱维（基础数值 + 1 效果）。作用 = 保证 3 选 1 **永远有一个安全无脑选**，不卡死。
4. **干扰 / 拒止 e（新增必备件）**：攻击对手的资源 / 连携（抽对手能量、锁对手道具槽、打断链条）。是**长链 / 回路在 PvP 的天然解药**，也是 PvP 反制与平均胜率的来源——与导出 e 同级，是结构件而非可选件。

---

## 8. 反固化设计

**目标**：玩家不应感到"走几步后必然只能玩一个流派"。手段：增加终点数 + 后移岔路口（提高路径重合度）。

- **主定理（§6）本身就是结构性解药**：系内不划算 → 玩家不会闷头钻一个方向 → 固化失去土壤。
- **导出网双解**：一套导出 e 同时"增加终点"（每维都是可达胜负手）+"后移岔路口"（抓桥接件后多条路仍开）。例：5 个导出 e（A→B/B→A/C→A/C→B/C→D）可派生 8 个等价答案，重合度高 → 选择变多。道具同理。
- **自然导出（弱于导出 e 但存在）**：快攻 = 攻够高当防；厚防 = 宽松攻磨死对方。允许玩家即便没导出 e 也能靠单体系取胜。
- **加权 draft（答 point 2，"潜在池"问题）**：**别按英雄硬锁道具池**（一选英雄就定 build = 岔路提前 = 固化）。正解 = 3 选 1 永远是全集，但**出牌概率向"能与当前 build 配合"的元件倾斜，并优先塞导出 / 泛连携 e**。英雄是**起手偏置，不是牢笼**。既有可读性（不选择瘫痪）、又什么都不锁、还主动后移岔路口。
- **变化道具困境的解（答 point 4）**：换英雄 = 离散 + 高方差（丢投资、随机塞新东西），找不到平衡点。把"变化"换成**连续 + 定向**就有解——而那东西就是**导出 e**：不丢投资的转向（乘在已有基础上、玩家主动选、低随机、高 agency）。**导出 e = 变化道具的低随机版本。** 分工：想要整队换血 → PvE 关卡间**招募/移除英雄**（安全节点）；局内变化 → 全交导出 e。
- **⚠ 反向失败模式：过度架桥 → 同质化**。导出 e 太多/太便宜 → 每个 build 都"什么都桥到" → 全收敛成一个全能大 build（固化的镜像反面）。导出 e 必须有机会成本，让你只桥得起 1-2 维。**甜点 = 够撑 2-3 个胜负手同时开着，不够让所有 build 长成一个样。** 落地数值红线。
- **多层连携（理论上限，留作参考）**：体系→攻防系数→复用 e 机制→资源级 层层相扣，选不同层构筑 = 不同答案。轻构筑难做到这么深，非本意，但多层连携是最大化收益的方法，且多层机制 e 大多能适配不同体系，理论备查。

---

## 9. 平衡哲学

- 平衡很重要，但**别过度平衡**：目标不是"所有选择 EV 相等"（那 = 选择不重要 = 无聊），而是 **无统治策略 + 风格可区分**。
- 不同 build 在打法 / 克制关系 / 风险曲线上不同；**用反制（干扰端）拉平胜率，不用同质化铺平强度**。
- **PvP combo 平衡看"扣除铺垫成本后的 EV"，不看 payoff**：1+1>2 是 payoff，但概率加权 + 成本调整后应回落到标准附近（高 roll vs 稳健的经典平衡）。
- **PvP 期望**：不同风格 build 大致平均地出现在对局中，并取得大致平均的胜率。

---

## 10. PvE × PvP：极大区分（同元件，不同容器规则）

差的不是道具本身，是**容器规则**：

| | PvE（远征） | PvP |
|---|---|---|
| 强度天花板 | 可允许**指数级**增长 | **严格限制** |
| 道具上限 | 可解开 | 锁 3 槽 |
| 对手 | 非对称 + 可设计针对性敌人 | 对称对手 + 干扰端 counterplay |
| 回路 / 长链 | 可放飞 | 必须压住 |
| 导出 e 价值 | 高（永久掉血刚需） | 低（单局血量重置） |

---

## 11. 单人远征骨架

> **任务8重置注记**：PvE 探索模式正式 GDD = `design/gdd/expedition-mode.md`（+四份子文档）。敌人继续按怪物系列体系落地，并允许多个地图敌人同格形成一名当前出战者加后备成员的遭遇队伍；"镜像小队"形态归入幽灵真人系与特殊遭遇。探索能量与旧版危险度成长均已作废，通用压力改由战斗间 HP 不恢复、有限治疗、敌人巡逻与死亡损失承担；"制裁特化 build"由动词考官系承担；"开局带 1 道具"=保险槽机制（expedition-backpack.md §4）。本节其余原则继续作为设计参考，但不得反向恢复已经作废的地图规则。

- 开局选 **1 名英雄** + 带 **1 道具**进场；后续可**招募 / 移除英雄**改变基础队伍；解开道具上限 → 闯关更远。
- **单英雄血量损失永久** → 对防御端形成强度要求（导出 e 因此在 PvE 成刚需）。
- **拿卡机会的路线设计**（参考尖塔地图）：路线选择 = 又一条 build 方向控制轴。
- **敌人 = 镜像小队**（与玩家形态一致；**极少 / 不存在多体同场**，区别于尖塔群怪）：
  - 可获特殊 buff（如**免费轮换** = 退一个弱动作；注意这是"+1 动作槽"的温和版，离通用 +1 槽梯度上很远）。
  - **有上限的成长**：随单局时长数值上涨但封顶 → 逼玩家重视进攻端（要打得够快），上限又保护体验不失控；**特殊 boss 可破限**，绝大多数敌人有限。
  - 应专门设计**制裁完全特化单端 build 的敌人**（反快攻只是其一，不止于此）。

---

## 12. 体系设计模板（参考，非束缚）

> 真正好玩的设计不应局限于模板，这只是参考脚手架。

1. 选定一个能力维度。
2. 在该维度上确定 build 核心机制。
3. 围绕该机制设计输入 / 输出 e。
4. 配对应的回路 e 与导出 e。

并区分：部分 e 围绕体系设计，部分 e 抛开体系单独设计（体系外点缀正是游戏真正的丰富内容）。

---

## 13. 决议与待办（2026-08-30 更新）

### 已定
- ⛔ **旧升级岔路已废止**：T1/T2/T3 成品均可直接构筑与抽取；通用升级、合成和图纸不再承担准入。
- ✅ **回路 e**：PvP 默认禁、只活 PvE；可实验性加入但**必须标注哪些是回路 e**，供测试再定（2A）。
- ✅ **道具使用时点**：揭示前随动作盲选提交，道具 = 主动计划件（3A）。
- ✅ **英雄主定位（2026-07-30 取代旧 A3）**：每名英雄最终只有一个进攻 / 防守 / 经济 / 调度 / 控制主定位；定位后置裁定，minor / 细枝设计下放给道具层。
- ✅ **核心 e**：不设"无核心 e"硬规——核心 e 可存在，主定理（§6）已压住其系内乘算、不逼固化（6C）。
- ⛔ **旧六维英雄配额（A3）已废止**：进攻 / 防御 / 能量 / 节奏 / 状态 / 干扰只暂留为道具加权技术标签，不再是英雄职位。
- ✅ **英雄规模与覆盖（2026-07-30）**：初始默认 **3** 名英雄，金币快速到 **≥12**；首发仍为 **24**，五种主定位数量允许不均，不再执行 6 维 × 4。旧“+1 起手道具”不沿用到 PvP，现行规则为双方三框空置开局。
- ✅ **英雄解锁节奏驱动 = 金币**（对局获取，初始 3 快速到 ≥12）。
- ✅ **数值地基（A2）**：能量收入 = **被动 +1 能/回合 + 攒额外 +1**（破解攒税、达标 1-3 分钟）；HP 保持 3-7；不变项见 §4.5。代码 D 阶段加被动 +1 + 大波改 3。
- ✅ **当前道具骨架**：T1/T2/T3 成品均可作为战备资产；PvP 三框空置开局、前置同步免费抽取、至多三件真实 UID 候选、放弃不重抽、锁定一回合、逐次 `0–4` 能使用费、有序施放、实例耐久与战末退包均已定。取得与实例边界已经封板。
- ✅ **五维平衡阶段 0+1 已通过**：机制价值使用下限 / 常态 / 峰值范围；逐次使用费、耐久与占格均逐件设计；临时件正常显示估值；经济以百万大赚、千万软上限为量级。具体道具属性尚未审批。
- ✅ **防御铁则 = 二元（WHETHER 非 HOW MUCH，2026-06-16 定死）**：分级整体挡（防→波 / 大防→波+大波 = 天花板）；**禁临时护甲 / 护甲吸收模型**（已否决）；穿透三档（穿防 / 穿大防[仅公开慢蓄 payoff] / 真伤）；打穿大防靠破防降级（龙 −1 级）+ 碎能 + 骗防；护甲 = 额外血量层；轻量治疗（道具）。详见 §4.6 + heroes-redesign 铁则块。
- ✅ **首发阵容 + BP（2026-06-16）**：**首发 = 24（h01–h24，12 生肖 + 12 暗生肖）+ 道具、pick-only 无 ban**（原 12 池容不下 ban——双方各 ban3+pick3 会用光全池）。

### 🔴 重大方向：英雄全面重设计
现有英雄 h01–h24 按本纲领一只一只重设计。核心原则 = **角色幻想先行、机制扎根基础动作、宽 combo、双向 yomi、主定位后置**；细微 / 次要效果归给道具层。

### 待办（数值 / 落地活，延后）
- [ ] **小型纵切批次**：先按五维卡设计约 15–20 件跨档样本，覆盖 0 费连接件、1–2 费主力件、3–4 费局势件，以及两件链、三件链与英雄呼应。
- [ ] **Demo 扩展池**：核心批获批后扩展到 50–100 件，再确定最终允许名单，不继承旧 114/163 件数量结论。
- [ ] **运行时迁移**：实例字段、通用免费三选一与准备规则、有序步骤、耐久、战末退包、搜刮、临时所有权、AI/UI/本地事件流/测试；PvP 协议与快照后置。

---

## 14. 道具设计判据（撞英雄 / 撞道具 · 2026-06-20）

> 两把尺子，配套 §4.6 真伤 4-gate；memory [[item-vs-hero-design-space]] / [[item-design-catalog]]。

**判据 A —— 道具撞英雄？看「计划身份与公共接口」**

> 同一原语，英雄是每回合反复改写选择的固有能力；道具是免费候选、公开锁定后再逐次付费、占用公开框并受实例耐久限制的工具。任何稀有度都可以一次性或耐用品，但仍必须扎根「波 / 攒 / 防 / 大波 / 大防」、能量、生命、切换、目标或公共状态，并保留空间、次数、期限、反噬或对手反制。

- 不能只把英雄技能装进物品；判断重点是它是否让不同英雄重新安排公共动作，而非是否出现了同一个触发词。
- 自检问句：**「这件道具是否提供一次新的计划选择；其代价、期限与反制是否仍让英雄固有能力保持独特？」**
- 真伤 4-gate（§4.6）= 本判据在「真伤」上的具体化。

**判据 B —— 道具撞道具（冗余）？看「决策身份与五维价格是否都重合」**

> 两件道具若在同一公共决策点、同一收益角色、同一触发和同一量级下没有不同顺序用途，再加上稀有度、耐久、占格与金币价格也没有形成真实取舍，就构成**有害冗余**。仅仅改数字、形状或名字不能自动把重复机制变成健康梯度。

- 战备方案会让不同稀有度共存，因此冗余审查必须**跨稀有度**进行；T3 尤其不能只是 T1/T2 的全面上位件。
- **纯数值放大只有跨质变阈值**（血线、击杀线、防御天花板）并产生新选择时才有意义；否则属于注水。

**判据 C —— 批次覆盖是否失衡？先查公共玩法，不按已有主题继续堆件**

> 已有组合信号同时可能代表「可连接」和「已饱和」。同一批候选不能把同一种资源（尤其护甲）同时当作缺口、连接轴和收益出口；否则看似每件都有 combo，实际仍是同一套结算反复换词。

- 提案前按「公共动作 / 能量 / 生命 / 切换 / 目标 / 命中触发英雄技能 / 道具栏操作 / 反制与信息 / 队伍调度」做覆盖矩阵；同一主题原则上不超过一批候选的约四分之一，除非本轮明确补建该体系。
- 每件候选必须写明它改变的具体行动选择和具体组合；「能吃到护甲、治疗或能量」只算兼容，不自动算 combo。
- 一批方案至少覆盖：直接公共动作计划、资源或节奏转换、对手可读的反制、队伍或目标调度，以及一种不依赖已有关键词的新结算结构。
- 若候选大量呈现「若 X，则 Y」，先合并重复结构，再从选择目标、交换、封顶、延迟兑现、资源迁移或一次性规则改写中寻找更鲜明的骨架。
- 护甲既是额外生命层，也是可被转移、转伤的资源。新增护甲件前必须同时检查各 tier 的生成、搬运、转化和三件叠用上限；当本批已经覆盖入口与出口时，优先转向其他玩法轴，后续审计发现真实层级缺口时仍可补齐。

---

## 15. 系统操作层（中立 · meta 子维度 · 2026-06-20）

> **定位**：道具作用对象 = **游戏系统的元素本身**（道具 tier / 槽 / draft 池 / 充能 / 电报 / 规则），而非对局资源（HP / 能量 / 动作）。它与现行旧六维技术标签正交，属于中立的系统操作层（吃本作独有结构、非干净原语、无英雄拿它当招牌）。
> **已隐性存在**：`封魔锁`(锁槽) / `圣人遗体`(偷道具) / `偷天换日`(换归属) / `迷魂药`(藏操作栏) / `乱点鸳鸯`(改指向) 早是 meta 件——本节把这层正式命名，空白格即可系统补齐。
> **2026-08-30 注**：下表中的升级池、部署锁、全局 draft 与充能 refill 是旧系统机会图，不再是当前获取骨架；可保留“操作真实背包、公开框、实例耐久、所有权和有序步骤”的设计空间，但具体机制须按新五维基准重新审批。

**可作用对象地图**（✅ = 空白可挖）：

| 对象 | 现状 | 空白 |
|---|---|---|
| 道具 tier | 三档成品可直接携带 | ✅ **临时改变一件道具的稀有度规则**（仅在确有新选择时） |
| 道具形态 | — | ✅ **道具燃料化**（烧道具换能量 / 伤害；§1 第三存量「道具」首次可燃） |
| 战斗道具区 | 容量与准备状态待重构 | ✅ **占位 / 弃置 / 保护一个实例** |
| 真实背包取得 | 每回合一次免费三选一、空框、锁一回合 | ✅ **检索 / 扩候选 / 改变补充规则** |
| 实例耐久 | 每回合一次、战末退包 | ✅ **转移 / 保护一次耐久**（Demo 暂不维修） |
| 基础收入 | — | ✅ **堵对手被动 +1 能**（区别猴 = 削存量、这 = 堵流量） |
| 槽 / 序列 / 指向 / 出战 | 已零散覆盖 | 多已占 → 只做一次性小版 |
| 回合数 / 结算顺序 / 动作槽 | 核心锁死 | ⛔ 禁（破 B-001）/ 至多 PvE boss |

**安全红线**：
1. **回路**：meta 件**禁自指 / 禁链式**（操作道具的道具不得作用于自己 / 同类 = 系内乘算回路 = PvP 炸弹 §6），违者标 PvE。
2. **总钥匙**（§8 固化）：强 meta 件**必带机会成本**（烧一件 / 占一槽 / 吃能量），不许变成「所有 build 都绕它」。
3. **能量闸门**（🔴 2026-08-30 当前规则）：标准抽取免费；任何让玩家降低、绕过或返还逐次使用费，以及复用或保护真实道具的 meta 件，都必须把省下的使用费、耐久、槽位和资产风险一起计价。
4. **核心规则**：操作结算顺序 / 回合数 / 动作槽 = 威胁 B-001 与「动作槽唯一稀缺」，基本禁、至多 PvE boss。
5. **避英雄招牌**：meta 对象上已站英雄的（出战 = 马狗羊 / 碎能 = 猴 / 基础攻击自由选敌 = 兔·2026-07-31 起；原「电报 = 房日」与“敌方重复动作产能”均已随 h04 历次重做失效），道具只做一次性小版（判据 A）。
